{
  "name": "nodemailer",
  "version": "1.0.0",
  "description": "sending email with node",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "me",
  "license": "ISC",
  "dependencies": {
    "nodemailer": "^6.6.1"
  }
}
